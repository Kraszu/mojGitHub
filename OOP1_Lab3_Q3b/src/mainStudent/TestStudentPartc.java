package mainStudent;

import view.StudentsView;

public class TestStudentPartc {

	public static void main(String[] args) {
		// here we start the student program
		StudentsView sV = new StudentsView();
		sV.louncher();
/* ArrayList are much more flexible in use compare to normal arrays.
 * ArrayList size can be easily change, we can add more elements whenever we want
 * to already existing list while the array have to have fix size and once created 
 * no extra element can be added.
 */
	}

}
